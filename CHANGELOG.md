# v3.0.0_Alpha-07

- 精简：守护设置两处文案去冗余括注（真机反馈）——① 「退避基数」说明移除尾注「（不再翻倍）；防空转由重试上限负责」，保留主体「自愈失败后固定等待 N 秒再重试」（行为变更属版本记录，设置页无需复述缘由）；② 「检测间隔（预设）」标题移除「（预设）」括注（该项与「自定义间隔…」入口并存，预设字样无区分作用）；2 字符串改动 ×5 语言 (版本号保持不变)

- 功能：两项自愈节奏设置（真机反馈需求）——① 新增「疑似断网检测间隔」（suspectIntervalSec，默认 0=跟随检测间隔，与历史行为严格一致）：检测失败但未达断网阈值（防抖中）时下一轮按此独立快间隔确认（预设 跟随/5/10/15/30/60 秒 + 自定义 5~600 秒弹窗，位于「检测间隔」项之后）——作用：防抖阶段加速确认断网（更快触发自愈）或更快发现网络自行恢复；恢复在线/判定断网后回到例行检测间隔；② 「退避基数」移除指数翻倍：自愈失败后固定等待所设秒数再重试（原为 基数×2^连续失败数翻倍、15 分钟封顶，现 30s 就等 30s，行为可预期）；防空转职责移交熔断重试上限（healMaxAttempts，已有设置）——连带清理：GuardSettings.maxBackoffPower 字段/键/默认值与 rememberGuardSettings 持久化整体移除（该字段本无 UI 入口，历史 prefs 残留键无害）；6 新字符串 ×5 语言 + 1 文案修改（退避 tip 改固定等待语义）×5 语言 (版本号保持不变)

- 调整：心跳闹钟看门狗改为默认关闭（用户要求）——第十一轮默认开启，现改回默认不启用，遇到「后台不检测」时再手动开启（显式开启过的用户不受影响，仅默认值变化）；开关子说明精简为「兜底保险（免特权）：检测停摆时补检，进程被杀后自动拉起」（原文案「深度休眠中唤醒锁会被系统忽略，闹钟仍周期唤醒补检测」对深度休眠穿透力表述过强——深度休眠中系统对每应用限流约 9 分钟一拍，闹钟按限流节拍而非设定值触发，且「默认开」声明已过时）；「后台保活」分组新增说明卡片（guard_heartbeat_banner，据用户问答结论浓缩）：心跳是兜底保险不是检测主力、深度休眠限流约 9 分钟一响且更短被静默顺延（接力棒式重排不排队）、短间隔买的是息屏初期与被杀后的复活速度、后台不检测时可开启；2 字符串改动（1 改 1 增）×5 语言 (版本号保持不变)

- 功能：心跳闹钟看门狗从「默认配置即生效」改为独立可关的设置项（用户需求：默认开启效果不变、可自由开关、归入「后台保活」分组，并可自定义一拍间隔）——① 新增设置「心跳闹钟看门狗」开关（GuardSettings.keepAliveHeartbeat，默认 true：默认效果与上一轮完全一致）置于设置页「后台保活」分组（唤醒锁之后），关闭后守护运行仅剩前台服务+START_STICKY+唤醒锁三层保活（深度 Doze 与进程被杀场景不再有闹钟兜底，onStartCommand 热加载路径即取消已排闹钟，不留「幽灵闹钟」）；② 新增设置「心跳间隔」（heartbeatIntervalSec，默认 0=自动：跟随检测间隔、60 秒硬下限，与历史行为逐条一致）：自定义档为预设 1/2/3/5/10/15 分钟（60 秒下限约束保留——深度 Doze 中 setAndAllowWhileIdle 每应用限流约 9 分钟一拍、更短被系统静默推迟不报错，短于 60 秒纯耗电无收益；正常态心跳触发时检测新鲜仅重排闹钟不重复检测，故自定义不影响检测逻辑，仅改变兜底频率与耗电）；设置页仅开关开启时显示间隔项，写入经 ACTION_RELOAD 热加载即时重排闹钟；6 新字符串 ×5 语言 (版本号保持不变)

- 修复：网络守护开启后应用处于后台不再检测（真机反馈：默认配置、未使用 Shizuku/Root 一键保活，应用退后台/息屏后检测循环停摆）——根因考证（AOSP Doze 官方行为）：Android 对未进 Doze 白名单的应用会忽略其持有的 PARTIAL_WAKE_LOCK，息屏静止约 30 分钟进入深度 Doze 后 CPU 照常深睡，守护主循环的协程 delay 定时器冻结——「后台不检测」；另有 MIUI/HyperOS 等国产 ROM 对未加白应用的进程查杀会拦截 START_STICKY 自动重启。现新增免特权心跳闹钟看门狗（默认配置即生效，无需任何保活命令）：① GuardService 新增 ACTION_HEARTBEAT，AlarmManager.setAndAllowWhileIdle（RTC_WAKEUP）自续期一拍闹钟，间隔取检测间隔与 60 秒下限的较大值——Doze 中照常触发（系统对每应用约 9 分钟限流一次、静默推迟不报错），触发时短暂唤醒 CPU 并给予临时白名单网络窗口；主循环失速超 1.5 倍间隔时补一轮检测，检测新鲜则仅重排闹钟不重复检测（非精确闹钟允许系统合并唤醒，兼顾电池）；② 进程被杀场景：PendingIntent.getForegroundService 使闹钟触发即可拉起服务重建前台服务与主循环（targetSdk=28 不受 Android 12+ 后台启动前台服务限制；系统 force-stop 会连闹钟一并取消，属系统级终止，仍需电池优化豁免或一键保活命令对抗）；③ 用户停止守护时 onDestroy 取消闹钟（防在途闹钟复活已停止的服务）；④ 深度 Doze 防误判：Doze 期间系统防火墙切断非白名单应用网络，探测失败不能归因于 WiFi 故障——探测失败且系统处于深度 Doze（PowerManager.isDeviceIdleMode）时如实记日志「本轮探测结果不可靠，暂不判定断网」，不累计失败计数、不触发自愈（避免重连后验证必失败污染统计并空转），退出 Doze 后下一轮恢复正常判定；⑤ START_STICKY 重启（intent=null）如实记日志「守护进程被系统回收，已自动恢复检测」，用户可据此判断后台存活状况；2 新字符串 ×5 语言 (版本号保持不变)
- 修复：已保存 WiFi「直连成功」用空密码污染破解历史（真机反馈：密码字典破解运行方式=应用API+授权定位+开定位，首次破解成功记录「连接成功：123456789」；之后已保存弹窗密码显示"*"，选「直接尝试/重新运行→沿用进度」后 wifi 列表显示「连接成功：」为空——系统已保存该 WiFi 走免密直连，连接成功但密码为空串，被 addOrUpdateHistory 整行 REPLACE 覆盖，历史「密码：」变空标签且字典列表被替换成 [\"\"]）——现空密码（=系统已存配置直连）成功只提示不写历史：绝不用空值覆盖既有真实密码记录，无既有记录也不新建（直连不产生密码信息）；运行列表提示改显示「连接成功（系统已保存配置直连，密码未验证）」如实区分「字典验证成功」与「免密直连」；直连任务的进度刷新（updateHistory）同步跳过（空密码无进度语义）
- 加固：空密码直连任务无论结果如何都不再触发任务移除时的 forgetNetwork——此前直连失败（如路由器暂不可达）会误删用户系统里既有的保存配置（应用未新建任何配置，删除无理）；沿用进度续跑的进度型更新本就保留成功密码（updateAttempt 不碰 successfulPassword），本轮确认无恙
- 修复：安卓系统对普通应用读取已保存配置返回的密码掩码"*"（WifiConfiguration.preSharedKey 被系统置为字面量星号，与真实密码无关）被当真实密码使用/展示（真机反馈：wifi管理器→已保存页密码行显示"*"，破解页「发现已保存密码」弹窗显示「密码: *」）——新增 isMaskedPsk 判定：管理器已保存页掩码视为不可见（回退显示本应用破解记录密码，无记录则占位符"-"加「不可见（需 Root/Shizuku）」标签，与 Android 10+ 官方限制的表现对齐）；破解页弹窗改显示「密码不可见（系统限制，已保存配置可直接连接）」（5 语言）；「直接尝试」对掩码走空密码直连路径（掩码不是密码，不拿去建新配置）；管理器扫描页连接入口同步掩码处理
- 修复：破解历史页历史遗留的空密码记录（此前直连污染写入的）显示占位符"-"，不再渲染成空的「密码: 」标签
- 版本：保持 v3.0.0_Alpha-07 / versionCode 6 不变（按要求）

- 移除：移动数据网络卡片「QCI」行及 Root AT 通道读取能力整体下线（上一轮新增，按要求撤下）——QciReader 模块（基带 AT 节点探测 + 3GPP TS 27.007 查询 + MTK +ESUO 双卡切栈）、ManagerController 节流调度与 qci/qciNeedRoot 字段、网络页移动数据卡 QCI 显示行、mgr_qci_need_root 字符串 ×5 语言全部移除，连带清理失效的 Dispatchers/withContext 导入；移除后移动数据卡片保留 状态/运营商/漫游 行，WiFi 卡片与 3 秒自动刷新不受影响（源码与资源 grep 校验 0 残留，编译通过）
- 版本：保持 v3.0.0_Alpha-07 / versionCode 6 不变（按要求）

- 新增：移动数据网络卡片新增 QCI 显示（QoS 等级标识，5G SA 下为 5QI——运营商量化你这条数据连接优先级/时延预算的核心参数，QCI 9=尽力而为、6=高优先级数据、5=IMS 信令等），显示的是网络实际下发的值（非终端请求值）。获取方案经联网考证：Android 公开 API 不暴露 QCI（Android 12 的 QosCallback 属系统 API 普通应用不可达），dumpsys 中也无此字段，Cellular-Z/Network Signal Guru 走高通 DIAG（MTK 无此接口）——故采用 Root 下直连基带 AT 通道发 3GPP TS 27.007 标准查询（+CGDCONT?/+CGCONTRDP/+CGEQOSRDP=cid 读网络实际下发 QoS，MediaTek +ESUO 4/5 切换双卡协议栈、会话结束复位；AT 节点自动探测 MTK ttyC*/高通 smd*/三星 ttyACM*/展锐 stty* 并缓存，现代高通无 AT 通道时如实显示 -）。必须 Root 才可显示（无 Root 显示「需 Root 后可显示」提示）；读取经 stdin 喂 su（规避 MagiskSU 吞参数问题）、带看门狗防挂死、成功 30 秒/无 Root 120 秒节流重读、切换数据卡立即重读、双卡按数据网络 PLMN 匹配选栈
- 优化：包体积从 6.3MB 减至 4.5MB（-29%）——① 排除 desugar_jdk_libs 2.x 携带的旧字符集转码表（tables/ 共 675 文件：Big5/GBK/EUC-JP/EUC-KR 等，本应用全部 IO 均为 UTF-8 永不触达，实测 -1.4MB）；② 开启资源裁剪 isShrinkResources（移除 219 条未引用的 appcompat abc_*/Material3 日期选择器等库字符串，-0.3MB；aboutlibraries 等经 getIdentifier 动态查找的资源以 res/raw/keep.xml 显式保留）；③ 关闭 v1 签名（minSdk 24 起系统仅校验 v2/v3 签名，v1 的 META-INF/MANIFEST.MF/.SF/.RSA 约 280KB 属死重，实测旧 APK 中 v1 本就未被校验）；④ 排除第三方库误打包的 .java 源文件与 kotlin-reflect 元数据 kotlin_builtins（本应用无该依赖）
- 优化：网络页卡片文字颜色稍微加重一丢丢（真机反馈偏浅）——网络名称（WiFi SSID/移动数据运营商名）与分割线下方的信号强度/链路速率/频段/IP 等实时信息文字（含 QCI 行），亮色主题向纯黑、暗色主题向纯白各拉近一小步（15%/24%），只提一档对比度不改变视觉层级，其他页面详情行不受影响
- 精简：守护设置「息屏保持检测（唤醒锁）」改为「唤醒锁」，说明「保持进程在后台运行。（部分系统可能导致杀后台）」句号并入括号（5 语言同步）
- 版本：保持 v3.0.0_Alpha-07 / versionCode 6 不变（按要求）

- 修复：已授予定位权限后切换「应用 API」通道无效果、标签仍显示「来源 Shizuku」，且重开 WiFi 管理器时标签先显「来源 API」又立即翻回「来源 Shizuku」（真机反馈）——根因经官方文档考证：Android 9 起应用层 Wi-Fi API 出数据的硬性条件除定位权限外还包括系统定位开关（官方原文 Location services are enabled on the device (under Settings > Location)），权限≠开关：开关关闭时 getScanResults 应用层读数恒为空列表，而 Shizuku/Root 特权读同一系统扫描缓冲不受此过滤，读取侧的静默降级落在 Shizuku 上，标签如实显示实际数据来源——表现为「切了却像没切」。三处协同修复：① 通道弹窗中系统 API 的可用状态判定补上定位开关检查（此前只查权限，把「定位服务未开启」误报成「可用」），状态分列为 缺定位权限 / 定位服务未开启 / 可用；② 选择系统 API 时权限到手后若定位开关未开，先引导开启——优先 Play services「一键开启」可解析弹窗（checkLocationSettings → ResolvableApiException），无 GMS（国产 ROM 常见）或不可解析时 Toast 提示并跳转系统定位设置页；开启后切换、不开不切换（杜绝切了通道标签来回跳变），弹窗内选择一律实时判定（从系统设置页返回后 remember 缓存不再滞后）；③ channelOrder 可用通道序列同样要求权限+开关双就绪，且指定通道当前不可用时不再置于尝试序列首位（省一次注定为空的读）。读取层多通道回退兜底保持不变（通道真正出不了数时数据仍可达，标签如实标注实际来源）
- 优化：网络页 BSSID 隐藏提示文案「开启系统定位或授权 Shizuku/Root 后可显示」改为「开启定位或授权特权通道后可显示」——与来源通道切换弹窗的通道术语统一，不再点名具体通道
- 版本：保持 v3.0.0_Alpha-07 / versionCode 6 不变（按要求）

- 优化：扫描页未进入不运行以省功耗——扫描页进入才触发系统扫描（原有行为保留），离开扫描页（切到已保存/网络页或退出管理器）立即取消在途的扫描轮询（此前切页后最长约 3 秒的轮询读取仍继续）；来源通道切换弹窗确认后只刷新当前停留页的数据：在扫描页切换 → 重扫，在已保存页切换 → 仅重读已保存配置（不再触发射频扫描——扫描页进入时会自行重扫，此为第七轮新引入的行为，已按省功耗要求收敛）；主动取消扫描不再误报「无结果」空态
- 修复：切来源为系统 API 时未弹出定位权限申请（真机反馈）——根因经官方文档考证：Android 12+ 上单独请求 ACCESS_FINE_LOCATION 会被部分版本系统直接忽略（不弹任何对话框，官方原文 If you try to request only ACCESS_FINE_LOCATION, the system ignores the request on some releases of Android 12）。现改为 ACCESS_FINE_LOCATION + ACCESS_COARSE_LOCATION 双权限同请求（RequestMultiplePermissions，通道弹窗与破解页共用链路同步修正）；定位权限就绪判定放宽为 FINE 或 COARSE 任一（Android 10 官方规则：targetSdk≤28 应用声明 COARSE 或 FINE 任一即可使用 startScan/getScanResults/getConnectionInfo 等 Wi-Fi API——用户在 Android 12+ 选「仅近似定位」时系统 API 通道同样可用，不再误判缺权限）；Manifest 补声明 ACCESS_COARSE_LOCATION；权限被系统设为「不再询问」（运行时申请静默秒拒）时弹窗引导前往 应用信息 → 权限 → 位置信息 手动开启
- 优化：网络守护状态页「实时日志」与统计页「事件历史」改为默认折叠（长列表默认展开挤占状态卡/统计瓦片空间），且折叠/展开状态不再随切换守护页 Tab 自动重置——根因：NavContainer 切页会销毁非当前页的组合，卡片内部 rememberSaveable 状态随页销毁而丢失，切回即重置为默认展开；现状态提升到 GuardScreen 层持有（整个守护页存活期间有效，含配置变更恢复）
- 版本：保持 v3.0.0_Alpha-07 / versionCode 6 不变（按要求）

- 修复：扫描页未保存、未破解网络输入密码连接成功后不出现「忘记网络」按钮——根因：连接结束后 performSaved() 重读了已保存配置（savedCache 已含系统自动保存的新网络），但扫描列表各条目的 savedInfo 仍是连接前的旧合并结果（null），已连接卡片的忘记按钮以 savedInfo 为数据源故不出现，须手动重扫才恢复。现已保存配置的重读收敛到 performSavedNow 统一处理：savedCache 每次更新即同步重合并扫描页标记（连接结束/忘记轮询/破解历史变化全路径覆盖），连接 finally 改为立即读+600ms/1500ms 两次延迟补读（系统侧配置写入与认证失败自动忘记的删除均异步生效，与忘记网络轮询确认同一策略）
- 新增：「来源 xxx」标签可点击——弹出数据来源通道切换弹窗（扫描/已保存两页共用）：每通道附实时可用状态（可用/未运行/未授权/未绑定/缺定位权限），选择不可用通道时自动申请对应权限——Shizuku 未授权拉起授权请求、Root AIDL 未绑定则绑定服务（首次绑定触发系统 Root 授权弹窗，轮询最长 10 秒）、系统 API 缺定位权限则发运行时权限申请；权限到手后切换并双页刷新，被拒/失败保持原通道不切换（读取侧本就有多通道静默回退，不会取不到数据）。「自动」选项实时显示当前将选用的通道（Shizuku → Root → 系统 API 首个可用）；无数据时标签显示指定通道名（此前不显示导致切换入口不可达）；切换写入破解设置「执行通道」与设置页同源；AppShizuku.request 增加失败回调供拒绝反馈
- 版本：应用版本号回退为 v3.0.0_Alpha-07 / versionCode 6（按要求重新发布，覆盖旧 Alpha-07 Release）
- 修复：扫描页「已保存」标签与已连接网络卡的「忘记网络」按钮仅在 Shizuku 通道才出现——根因：扫描列表在指定通道结果为空时会静默降级补读其他通道，而已保存配置却只读单通道：指定「仅系统 API」时 Android 10+ 官方限制 getConfiguredNetworks 对所有应用返回空列表（与 targetSdk 无关），savedInfo 恒为 null → 已连接卡既无连接按钮（已连接按设计隐藏）又无忘记按钮（缺 networkId 数据源）。现已保存配置读取改为与扫描列表同一策略的多通道回退（fetchSavedConfigs：指定通道空 → 静默降级补读其他通道），「已保存」标记与「忘记网络」按钮全通道可用。连接/忘记写操作本身三通道均支持（targetSdk 28 豁免：AOSP WifiServiceImpl 对 targetSdk<29 应用放行 addNetwork/removeNetwork/enableNetwork，社区实测确认），写操作仍按设置指定通道下发；纯系统 API 且无特权通道在 Android 10+ 上因官方读限制无法取得 networkId，忘记按钮不显示属系统限制如实呈现
- 修复：错误密码连接报「超时」而非「认证失败」且不自动忘记（Alpha-27 遗留）——根因经 AOSP 源码考证：① 官方认证失败广播未使用：系统 WiFi 设置显示「密码错误」的同源信号是 SUPPLICANT_STATE_CHANGED_ACTION 广播携带 EXTRA_SUPPLICANT_ERROR = ERROR_AUTHENTICATING（wpa_supplicant 判定 4 次握手失败/关联被拒为密码错误 → AUTHENTICATION_FAILURE_EVENT → 紧随的状态广播打上认证失败标记），现已实时监听该广播（sticky 残留防护：注册后 800ms 内的回调全部丢弃），密码错误最快约 1 秒即报「认证失败」；② SCANNING 状态被漏判：密码错误重试循环中 supplicant 主状态为 SCANNING（AOSP SupplicantState.isHandshakeState 不含 SCANNING，框架握手环回 >4 次禁用网络后更停留在 SCANNING/INACTIVE），原失败判定只认 DISCONNECTED/INACTIVE 导致信号永远凑不齐而等满 20s 超时；③ 无定位权限盲区：SSID 被屏蔽为 <unknown ssid> 时关联判定恒 false，现特权通道解析出目标 SSID 且握手未完成同样计入关联回合（≥3 回合判败）；④ 超时兜底自动忘记：个别 ROM 不产生标准失败信号时，超时且本次为应用新建的配置同样自动忘记，错误密码配置绝不残留系统（已存在的用户配置仍不误删）
- 新增：扫描页已连接网络卡片显示「忘记网络」按钮（带二次确认对话框）——重复连接无意义但忘记（如路由器改密码后重连）是高频需求；忘记后扫描列表「已保存」标记同步消失
- 优化：网络页多网络卡片自动折叠后的展开/收起补上流畅动画——高度变化用 animateContentSize 无弹跳平滑过渡（与扫描页卡片同款成熟方案），折叠指示箭头同步无弹跳旋转 180°（原为静态箭头瞬间切换）
- 版本：应用版本号回退为 v3.0.0_Alpha-07 / versionCode 6（按要求重新发布，覆盖旧 Alpha-07 Release）

# v3.0.0_Alpha-27

- 优化：扫描页/已保存页「当前网络」卡片高亮色柔化——原纯 primaryContainer 高亮真机观感过于鲜艳（虽跟随主题但饱和度过高），现改为 primaryContainer 与 surfaceContainer 按 35:65 混合取色，仍随主题变化但观感柔和，两个页面统一
- 精简：扫描页卡片头部移除「2.4G CH3」频段信道标签与「当前」标签（下划线上方区域）——频段/信道信息在展开详情中保留完整行，「当前连接」状态由卡片高亮色表达，头部只保留加密类型/已保存/已破解标签
- 网络页：移除「网络诊断」卡片（探测能力保留在网络守护页完整可用）；WiFi 与移动数据卡片新增每 3 秒自动刷新——信号强度/链路速率/验证状态/IP 等动态数据实时更新，概览行显示「每 3 秒自动刷新」提示，手动刷新按钮保留
- 修复：输入密码连接错误密码时无限「连接中」且无错误提示、错误密码配置留存系统——连接成功判定收紧为身份匹配（SSID/netId）且 SupplicantState.COMPLETED（官方语义：4 次握手成功完成；错误密码时关联阶段 SSID 已可读，仅凭 SSID 匹配会误报成功，即「提示已连接实际永远连不上」的根因）。新增密码错误快速判定：目标已关联却反复断开（wpa_supplicant reason=15 4WAY_HANDSHAKE_TIMEOUT 同源信号，连续 2 次）或关联尝试回合达 3 次，立即报「认证失败：密码错误或被网络拒绝」；本次新建的配置自动忘记（防止错误密码留存导致下次连接按旧配置重试失败），已存在的用户配置不误删
- 优化：短密码连接报「连接请求失败：添加网络失败」冗长且无意义——新增密码长度预校验（与系统 Settings 同规则）：WPA/WPA2/WPA3 需 8~63 字节（64 位十六进制亦合法）、WEP 需 5/13 位或 10/26 位十六进制，不合规在密码框即时标红并提示具体规则，确认时 Toast 说明原因且保留对话框供立即修正，不再下发注定被系统拒绝的请求；系统拒绝配置（addNetwork 返回 -1）时给出可读原因「密码长度或加密方式不匹配」
- 多语言：8 条新字符串 × 5 语言、移除废弃「网络诊断」2 键 × 5 语言（简体中文 / English / 繁體中文 / 文言文 / en-rCN）

# v3.0.0_Alpha-26

- 修复：扫描页卡片已连接状态「塌方」（布局崩坏、内容叠压）——Alpha-25 的展开动画改用 AnimatedVisibility(expandVertically + fadeIn)，在连接成功瞬间的状态切换（卡片换色 + 「当前」标签插入触发重组 + 广播触发的列表刷新）下会破坏布局测量。现改为常驻组合 + animateContentSize 容器尺寸动画（无弹跳 spring，与守护页通道行同款成熟方案），状态切换仅是普通重组，布局稳定；同时按用户要求移除「弹性旋转指示箭头」（保留圆角水波纹修复）
- 优化：扫描页展开卡片的「连接」按钮对已连接网络不再显示（重复连接无意义，卡片头部已有「当前」标识；未连接网络保持原有连接入口）
- 修复：已保存页密码行文案重复（「密码 · 不可见（需 Root/Shizuku）不可见（需 Root/Shizuku）」显示两遍）——值区改显占位符「-」，解释性小标签仅在右侧出现一次；开放网络如实显示「开放网络，无需密码」（不再误标「需 Root/Shizuku」）；顺带补齐已保存条目的加密类型识别，使已保存页的开放网络也能走免密直连
- 新增：认证 WiFi（Captive Portal）连接提示——扫描页/已保存页连接成功后自动检测（NET_CAPABILITY_CAPTIVE_PORTAL 能力位，即系统「登录到网络」通知的同一信号源，连接后轮询约 16 秒），命中后弹窗说明该网络需完成网页认证才能上网，弹窗内含「前往系统 WiFi 设置」按钮直接跳转系统设置页；网络页 WiFi 卡片同步显示「疑似门户认证」标签
- 重构：网络页卡片支持多网络同显——一般用户单网络（WiFi 或移动数据）显示完整详情不变；WiFi 与移动数据同开（或个别系统双 STA 同时连接多个 WiFi）时每网络一张卡片同屏全部列出，自动折叠分割线下方的信号强度/链路速率/IP 等详情（点击卡片展开，静态指示箭头无弹性动画），概览行显示已连接网络数；移动数据卡标题为运营商名、副标题为连接状态，WiFi 卡标题为 SSID；全页术语统一为「移动数据网络」

# v3.0.0_Alpha-25

- 修复：已保存页「忘记」后条目仍残留（需切换页面才消失）——系统侧删除是异步生效的，忘记命令返回后立即重读常仍读到旧配置。现忘记后自动轮询重读（最长约 4 秒，静默模式不闪 loading 转圈），确认目标网络已从系统配置移除后原地更新列表；同一 SSID 若另有破解记录则保留为纯记录条目（按钮变为「删除记录」），属预期行为
- 新增：扫描页展开卡片「连接」按钮——已保存网络（含密码不可见的 Android 10+ 场景）按 networkId 直连；留有破解密码的直接带密码连接；开放网络免密直连；未保存的加密网络弹出密码输入框。连接复用已保存页的完整生命周期（WiFi 自开、范围外预检、真实结果验证），连接成功后系统自动保存配置无需再单独保存，全程按钮 loading 并锁定防重复触发
- 新增：网络页当前连接卡显示「移动数据网络」信息（连接状态 / 系统验证结论 / 运营商 / 漫游）——全部采用免权限 API（ConnectivityManager 蜂窝能力位 + TelephonyManager 运营商名），无需 READ_PHONE_STATE；新增蜂窝 NetworkCallback，移动数据连接/断开实时刷新（网络诊断功能不改动）
- 修复：网络页 BSSID 显示 02:00:00:00:00:00 令人困惑——该值是定位服务关闭等场景 WifiInfo.getBSSID() 返回的系统匿名化占位 MAC（非真实路由器地址）。现检测到占位值时如实显示「BSSID 已被系统隐藏（开启系统定位后可显示）」，真实 BSSID 可见时正常展示
- 优化：扫描页卡片展开/收起动画与点击反馈——展开区改为高度展开 + 淡入双重过渡动画（此前内容无过渡瞬间拼入）；点击水波纹修复为跟随卡片圆角形状（此前为圆形外扩与圆角卡片不符，改用 Card(onClick) 重载自动裁剪）；标题行新增展开指示箭头（点击时弹性旋转 180°，提供可展开的视觉线索）
- 修复：扫描页网络图标与密码字典破解运行页不一致（材质单色 WiFi 图标 vs 四格分档信号图标）——改用同一 WifiIcon 组件与同一分档算法（signalLevel，分界 [-88,-77,-66,-55] dBm 与 AOSP config_wifiRssiLevelThresholds 官方阈值完全一致，信号条强度检测与系统一致）
- 修正：网络守护 ICMP ping 探测子说明「需 Shizuku/Root」与事实不符——AOSP init.rc 明确写入 net.ipv4.ping_group_range="0 2147483647"（全 uid 可创建非特权 ICMP 数据报套接字），应用进程内直跑 ping 无需任何特权通道（真机已实测验证）。删除该误导性表述；自愈执行通道说明卡片同步修正（特权通道仅自愈命令必需，ICMP 优先特权 shell、无特权时应用内直跑）
- 多语言：13 条新字符串 × 5 语言 + 2 条文案修正 × 5 语言（简体中文 / English / 繁體中文 / 文言文 / en-rCN）

# v3.0.0_Alpha-24

- 修复：Logcat 日志模式「直接尝试」永远卡在运行中（进度 0/1 0.0%，反复 已连接→未连接→已连接）——logcat 能否读到 wpa_supplicant 日志取决于执行通道身份（root/shell 可读，普通应用自 Android 4.1 起无 READ_LOGS 被拒）且部分设备根本不输出该日志，日志流零事件时结果判定只能干等整体超时，叠加默认「重试次数=无限」形成无限重连循环。现 Logcat 模式并行启动广播监听兜底（双流合并，先到先判）：成功/失败判定不再单点依赖日志解析，logcat 保留握手计数/计时能力（握手超次判定仍需日志流）
- 修复：广播监听成功判定改为 SupplicantState.COMPLETED——官方语义为「全部认证流程完成，WPA2 下即 4 次握手成功」，即密码正确的最早可靠信号，不再等待网络层 CONNECTED（DHCP 完成），判定更早且不受个别设备 DHCP 慢导致超出密码超时误判的影响；网络层 CONNECTED 保留作为 COMPLETED 的兜底信号
- 修复：广播成功事件携带实际连接 SSID 校验——定位可用时读取 WifiInfo 实际 SSID 与目标比对，防止尝试期间系统自动重连其他已保存网络造成误判成功；读不到实际 SSID 时沿用目标 SSID 假设（与历史行为一致）
- 修复：认证失败（ERROR_AUTHENTICATING）不再限定「密码超时」判定模式才生效——认证失败即密码错误属定论信号，所有判定模式下立即判负，错误密码不再干等握手超时/整体超时，破解循环显著提速
- 修复：Logcat 日志流健康监测——流式命令本不应自行退出，异常立即结束（执行通道未连接/无权限/命令错误）或 30 秒零输出时，在运行日志中明确提示通道状态与兜底情况，不再静默失败让用户排查无门
- 修复：Logcat 成功判定双模式——新增 wpa_supplicant 标准事件 CTRL-EVENT-CONNECTED（跨版本稳定字符串）与「WPA: Key negotiation completed」互补提升设备覆盖面；成功事件在起始行（Trying to associate / enableNetwork）未匹配时改用日志行到达时间与目标 SSID 兜底，避免被新鲜度检查误丢弃（此前该场景下即使成功日志已到达也会被判为过期事件丢弃）
- 多语言：3 条新字符串 × 5 语言（简体中文 / English / 繁體中文 / 文言文 / en-rCN）

# v3.0.0_Alpha-23

- 修复：密码字典破解成功后 WiFi 立即断开——成功路径此前仍执行 cleanConnection：API29 系统通道（WifiNetworkSpecifier）注销回调即释放请求来源的网络（官方文档：unregisterNetworkCallback "possibly releases networks originating from a request"），且 onAvailable 内自注销回调等于连上即拆；Shizuku/API 通道在「断开方式」为 Shizuku/系统 API 时主动 disconnect/disableNetwork 同样立即断开。现成功后保持连接不断开，仅失败/超时/中断复位；specifier 请求改为进程级单例管理，新请求发出前先释放旧请求（防新旧请求争夺射频来回切换），cancelWifiRequest 幂等化（重复注销的 IllegalArgumentException 此前会沿调用链向上中断成功结果处理）
- 修复：本应用破解成功的 WiFi 不出现在管理器已保存页——三处协同修复：① API29 通道成功路径被重复注销回调的异常中断，破解密码从未写入历史；② 历史进度更新（updateHistory）用 REPLACE 写入 password=null 覆盖已成功密码，重新破解同一 SSID 会清空旧密码，新增 upsertAttempt 进度型更新绝不触碰 successfulPassword；③ 已保存列表读取改为直读 StateFlow.value 并监听破解成功 SSID 集合变化自动重读（compose 状态首帧未传播首次发射时此前会漏掉破解记录）
- 修复：已保存页「当前」高亮不实时——系统关闭 WiFi 重连后高亮不更新（currentInfo 仅进入页面时读取一次）。现管理器存活期间注册 WIFI_STATE_CHANGED / NETWORK_STATE_CHANGED 广播 + WiFi NetworkCallback 双通道实时监听（500ms 防抖），CurrentNetworkInfo 新增 netId 字段，高亮改为 SSID 或 networkId 双匹配（定位关闭时 netId 经特权通道解析，与定位开关无关）
- 修复：已保存页点击「连接」无论成败总提示「√ 网络名」——连接命令发出即报成功。现完整连接生命周期：WiFi 关闭时先经通道开启并等待就绪（最多 10s，失败明确提示）；下发连接后轮询验证真实结果（应用层 WifiInfo 快速校验 + 特权通道 3s 节流解析，SSID/netId 双匹配，20s 超时）；约 7s 起用扫描结果预检——目标不在附近且尚未连上则明确报「未在附近扫描到，可能不在范围内」不必等满超时；连接中按钮显示进度并锁定，结果 Toast 如实区分已连接 / 无法开启 WiFi / 不在范围内 / 超时（密码已更改）/ 请求失败
- 新增：已保存页筛选（全部 / 系统保存 / 破解记录）与排序（名称 A→Z / 名称 Z→A / 最近破解优先）——FilterChip + 排序菜单，选择跨页面切换保持
- 新增：已保存页右侧 A-Z 字母索引栏（通讯录风格标配）——SSID 按拼音首字母分组（汉字经 TinyPinyin 转拼音，数字/符号归 # 组），字母分组吸顶标题（stickyHeader 含每组数量）；索引栏当前分组字母高亮放大、拖动滑动实时联动滚动、无条目字母淡显且拖动时就近落位、拖动时中央显示大字母气泡；「最近破解优先」排序为时间平铺列表时自动隐藏索引栏
- 多语言：18 条新字符串 × 5 语言（简体中文 / English / 繁體中文 / 文言文 / en-rCN）；删除孤儿键 mgr_pwd_from_pojie（已由 mgr_cracked_mark 徽标替代）

# v3.0.0_Alpha-22

- 修复：主页网络状态卡缓存新增 WiFi 会话句柄检测——`Network.getNetworkHandle()` 每次连接重新分配且与定位开关无关，关闭 WiFi / 切换 WiFi / 断开重连 / 切换到移动网络时状态即时刷新；定位关闭期间切换 WiFi 不再显示旧网络名（此前最长需等待 60 秒过期刷新），新会话立即经特权通道解析
- 新增：WiFi 管理器完整实装（原「扫描 / 本地 / 设置」三页均为未完成占位）——
  - 扫描页：信号四档分级（dBm 与主流分析工具一致分界）、SSID / BSSID / 频段 / 信道（2.4G / 5G / 6G 信道换算）/ 加密类型 / dBm 数值展示，当前连接高亮、已保存与已破解徽标，点击展开完整详情与一键复制；空态区分「WiFi 未开启」（可一键开启）与「未扫描到网络」
  - 已保存页：系统保存网络（特权通道读取，含明文密码）与本应用破解记录（本地数据库，无任何权限要求）合并展示；密码分级可见——特权明文 > 破解记录 > 不可见（如实标注「需 Root/Shizuku」，Android 10+ 官方限制 getConfiguredNetworks 对所有应用返回空）；密码眼睛切换与复制、连接、忘记（确认对话框）、删除破解记录
  - 网络页：当前连接全量详情（SSID / BSSID / RSSI / 链路速率 / 频段信道 / IP / 网关 / DNS / DHCP / 租约 / 系统验证状态，SSID 经三级解析不受定位限制）+ 四段信号仪表条 + 一键网络诊断（HTTP 204 / DNS / ICMP / 系统验证四项探测，复用网络守护探测引擎）
  - 通道体系：沿用破解页「扫描通道」设置并在结果为空时自动降级补一次其他可用通道（Shizuku / Root AIDL / 系统应用层），实际数据来源在页面如实标注
- 新增：扫描结果频段字段全链路支持（Shizuku 反射 / Root AIDL / 系统应用层三通道均新增 frequency 读取）
- 多语言：WiFi 管理器 43 条新字符串 × 5 语言（简体中文 / English / 繁體中文 / 文言文 / en-rCN）

# v3.0.0_Alpha-21

- 修复：主页网络状态卡 cmd 特权解析从未生效——原解析的 `Current network:` 输出格式在 AOSP 中并不存在（考证 packages/modules/Wifi 源码 WifiShellCommand#printWifiInfo），真实格式为 `Wifi is connected to "SSID"` 与 `WifiInfo: SSID: …, Net ID: …`；现按 AOSP Android 11~16 真实输出重写解析（含 SSID 带引号/十六进制/被屏蔽三态、SSID 含逗号截取 `, BSSID:`、`Wifi is not connected` 不误匹配），12 组实测格式用例离线验证通过
- 修复：网络名显示后回落「WiFi 已连接」的振荡——定位服务关闭时系统将 WifiInfo 的 networkId 一并屏蔽为 -1（AOSP：mNetworkId 随 REDACT_FOR_ACCESS_FINE_LOCATION 置 INVALID_NETWORK_ID），原缓存按 netId 匹配永远落空；现改为会话级缓存：同一次 WiFi 连接内不要求 netId 匹配、仅在断开时清空，定位关闭期间显示稳定不再跳变
- 优化：定位关闭期间切换 WiFi 的检测——缓存超过 60 秒自动经特权通道刷新一次（netId 被屏蔽无法直接比对换网）；定位开启时 netId 不一致立即重解析；多次网络回调刷新串行化，防止慢的解析结果乱序回写
- 说明：网络名获取优先级保持不变——应用层直读 → 缓存沿用 → 特权命令（cmd wifi status → dumpsys wifi，遵循「执行通道」）→ 兜底「WiFi 已连接」；移动数据等其它网络类型显示不变

# v3.0.0_Alpha-20

- 新增：主页网络状态卡网络名三级获取——优先应用层读取（现方式），拿不到时经「执行通道」特权命令解析（cmd wifi status → dumpsys wifi，不受定位开关限制；同一网络沿用已解析结果、命令调用 10 秒节流，显示稳定不跳变），仍拿不到兜底显示「WiFi 已连接」；移动数据等其它网络类型显示不变
- 新增：主页快捷操作「网络守护」入口——横向卡片样式与现版本一致（绿色系配色，心电监护图标），位于破解 / 管理器竖卡组下方首位，点击直达网络守护页

# v3.0.0_Alpha-19

- 回滚：主页网络状态卡恢复上一版实现——撤销 Alpha-18 引入的三层 SSID 解析（同网络缓存沿用 + 特权通道兜底），状态卡显示行为与 Alpha-17 一致
- 回滚：主页快捷操作恢复上一版布局——撤销 Alpha-18 引入的 Apple 主屏网格瓦片与「网络守护」入口，恢复「破解 / 管理器」双竖卡 +「检测 / 设置」双横条布局
- 说明：Alpha-18 其余功能（守护日志去重、按钮长按提示气泡、卡片展开收起动画）保留不变

# v3.0.0_Alpha-18

- 修复：每次开启守护出现两条内容相同的「在线 ✓」日志（相差约 2 秒）——服务启动时注册的网络状态广播接收器会立即收到一次系统粘性广播回放，与「开启即首轮检测」几乎同时各跑一轮；现增加最小检测间隔（距上次检测完成不足 5 秒的非手动检测直接跳过），网络事件触发的即时检测能力不受影响
- 修复：主页网络状态卡在「WiFi已连接」与「已连接 xxx」之间跳变——应用层 WiFi 名称受系统定位开关限制（关闭时恒为 unknown），现按「应用层读取 → 同网络缓存沿用 → 特权通道兜底（cmd wifi status，不受定位限制）」三层解析并缓存，同一网络显示稳定一致（断开重连自动重新解析；特权解析 10 秒节流避免高频网络回调反复执行命令）
- 新增：实时日志 / 事件历史卡片全部功能按钮支持长按提示气泡——长按任意图标按钮在其正下方弹出深色圆角气泡显示按钮名称（自动居中并夹在屏幕内，底部空间不足时翻转到上方，约 2.5 秒自动消失，全局同时只显示一个）
- 新增：实时日志 / 事件历史卡片展开 / 收起按钮（工具行末尾箭头图标，箭头随状态旋转 180°，卡片内容以弹性展开 + 淡入淡出动画收放，收起后仅保留标题与按钮行；状态跨页面保留）
- 重构：主页快捷操作改为 Apple 主屏风格网格——「网络守护」新增入列，破解 / 守护 / 管理器 / 检测 / 设置 五个 60dp 圆角方块图标瓦片（4 列等宽 + 下方短标签，末行左对齐），按压无涟漪、瓦片弹性缩放（iOS 图标按压反馈）

# v3.0.0_Alpha-17

- 新增：自动保存日志——设置页「日志」分类新增「自动保存日志」开关，守护运行时新日志自动追加到「日志保存位置」（每天一个 guard-auto-日期.log 滚动文件，自动保留最近 30 个，跨天自动清理；每轮检测后落盘，守护停止时最终落盘一次；中途开启会自动补写当前缓冲内全部日志）
- 优化：Shizuku / Root / 恢复默认保活结果改为只显示最近一次操作的单条反馈——原三次操作的结果行会叠加堆放（最多 3 行）；「恢复默认」结果去掉冗余的「恢复默认：」前缀，全部恢复显示「✓ 已恢复默认」，部分恢复显示「已恢复 N/4 · 未恢复：…」
- 修复：状态页 Hero 状态卡「未运行 / 链路断开」的灰色不随主题变化——现改用主题 secondary 色，所有状态色均跟随总设置中的动态主题色/颜色种子；首页大状态卡（网络状态）图标与文字原为硬编码蓝色，现同样跟随主题色
- 调整：统计页「清零」按钮移入事件历史卡片工具行（复制 / 保存 / 分享 / 管理 / 清空事件 / 清零），与「清空事件」相邻，保留清零二次确认弹窗
- 精简：「常驻守护通知」说明精简为「开启后始终常驻通知」；「息屏保持检测（唤醒锁）」说明改为「保持进程在后台运行。（部分系统可能导致杀后台）」

# v3.0.0_Alpha-16

- 修复：后台保活 Shizuku 通道恒显示 0/4——Shizuku 的 newProcess 是 exec 语义（不经 shell 解析），含 ";" 的回读校验脚本被按空格拆成参数（"whitelist;" 被当作 dumpsys 的普通参数），回读全部落空；现改为 sh -c 单次 shell 执行整段脚本（与 Root 的 su -c 同构），命令实际早已生效，仅校验误报
- 修复：后台保活 FGS 项恒显示未生效——前台服务 appop 的正确名称是 START_FOREGROUND（AOSP AppOpsManager 定义，默认模式 allow），此前误写作 START_FOREGROUND_SERVICE（AOSP 从不存在该 op，set/get 均报 Unknown operation 静默失败）；RUN_ANY_IN_BACKGROUND / START_FOREGROUND 两项 op 仅 Android 9+ 存在，更早版本按"已放行"处理不再误报
- 修复：「恢复默认」恒显示 1/4 未恢复——AOSP 行为：appops set <op> default 会删除该 op 的显式条目，之后 get 输出 "No operations." + "Default mode: allow"；恢复校验现把"无显式条目"判定为已恢复（此前只找 "OP: default" 行，必然找不到）；Doze 白名单判定同步兼容 Android 9+ 的 CSV 行格式（"user,com.foo,true"，原逐行等值匹配永不命中）；回读脚本改用 echo 分段标记，Doze 与三个 appop 各段独立解析
- 优化：开启守护立即执行首轮检测（移除固定 3 秒等待；WiFi 未就绪由跳过分支与防抖阈值兜底），「立即检测」即时响应
- 优化：自愈指数退避改为时间戳门槛，不再占用检测互斥锁——原实现在锁内 delay 最长 15 分钟，退避期间「立即检测」会被互斥锁卡住；现退避期间检测照常进行（每窗口仅记一次跳过日志），手动检测/网络恢复/自愈成功即复位退避
- 调整：统计页「清零」按钮移至页面底部（远离大数字瓦片区），并增加二次确认弹窗（全部统计与事件历史清零不可恢复）

# v3.0.0_Alpha-15

- 新增：自动清理实时日志与事件历史——设置页「日志」分类新增「自动清理」（关闭 / 保留 1 / 3 / 7 / 30 天），超过保留天数的实时日志与事件历史自动删除；实时日志在检测轮次与状态页打开时清理，事件历史在记录新事件与统计页打开时清理（服务未运行也能清）
- 重构：「状态」页改版（参考 Apple 系统状态页与 iOS 设置风格）——顶部 Hero 状态卡（大圆角图标 + 大号状态结论 + 上次检测时间，随状态整体着色）；守护开关与「立即检测」合并为 iOS 分组列表式控制卡（居中主色动作行）；探测项与执行通道合并为「检测详情」分组卡（行间 hairline 分隔线 + 状态圆点，保留点击展开 / 长按复制交互）
- 重构：「统计」页改版（参考 Apple Health「收藏」卡片风格）——四项统计改为 2×2 大数字瓦片（彩色圆角图标 + 大号数字 + 灰色标签）；动作有效率卡标题行新增总成功率，每动作行改为「名称 + 次数 + 百分比」+ 细圆角进度条；事件历史列表改为 iOS 分组列表样式（行间分隔线，时间/SSID 与结果行分层）
- 修复：「息屏保持检测（唤醒锁）」开关未持久化（切换后重启应用回到默认开启状态）的问题
# v3.0.0_Alpha-14

- 新增：后台保活「恢复默认」按钮——一键撤销保活命令（Doze 白名单移除 + 三项 appops 重置 default）并回读校验；自动选通道执行（Shizuku 可用走 Shizuku，否则直接 su 经 Magisk/KernelSU 授权）
- 优化：保活执行结果语义化显示——全部生效时只显示「✓ 全部生效」，部分生效才显示「生效 N/4 · 未生效项」（Doze/RUN_ANY/RUN/FGS 技术名保留用于排障），结果行着色区分（全部生效为主题色）
- 精简：移除 5 处说明卡片（探测方式"已连接不等于…"、自愈策略"假连接选标准档…"、高成功率档"当前优选动作…"、退避上限"连续失败达上限后…"、统计页"有效动作即故障诊断…"）；「执行通道」说明精简为"仅自愈与 ICMP 探测用特权通道；HTTP/DNS/能力位走应用层"
# v3.0.0_Alpha-13

- 优化：自定义自愈档的动作组合改为弹窗编辑（原平铺 5 行勾选项占用设置页过长）——「自愈动作组合」单行入口，弹窗内勾选并保留每个动作的轻重说明与执行顺序提示，确认后按由轻到重固定顺序存储
- 优化：Root 一键保活改为独立直连通道——直接 su -c 在同一 root shell 单次跑完全部保活命令与回读校验，不再依赖应用内 Root 服务（无需先连接 RootAIDL，有 Magisk/KernelSU 授权即可；网络调研确认无 root 专属额外命令，deviceidle whitelist + appops 组合即通行做法）；授权失败时明确提示"未安装 Magisk/KernelSU 或已拒绝授权"
- 精简：移除 3 处说明类卡片/提示（记录日志类型弹窗内 tip、日志保存位置"默认存…"卡片、后台保活三级说明卡片），设置页更紧凑
# v3.0.0_Alpha-12

- 新增：后台保活三级方案（解决息屏/后台后不检测不自愈）——设置页新增「后台保活」分组：
  - 系统级①「息屏保持检测（唤醒锁）」：持有 PARTIAL_WAKE_LOCK 防止息屏后 CPU 深睡暂停检测协程（默认开启，可关）；设置热加载即时生效，服务停止时释放
  - 系统级②「电池优化白名单」：拉起系统豁免弹窗（免特权），回前台自动刷新豁免状态
  - Shizuku / Root 一键保活：执行 dumpsys deviceidle whitelist +<包名>（Doze 白名单）、cmd appops set RUN_ANY_IN_BACKGROUND / RUN_IN_BACKGROUND / START_FOREGROUND_SERVICE allow（国产 ROM 后台查杀关键开关，网络调研验证的通行做法），执行后回读校验并逐项显示 ✓/✗（Doze · RUN_ANY · RUN · FGS）；Shizuku 未授权先弹授权，Root 未连接自动拉起
- 修复：WiFi 总开关循环（wifi cycle）后部分 ROM 不自动回连上次 WiFi——重新开启后轮询观察 8 秒，仍未回连则按当前执行通道显式定向重连（Android 11+ 用 cmd wifi connect-network 已保存网络免密码；全版本兜底 netId enableNetwork 定向连接）
- 优化：「记录日志类型」弹窗选项改为横向流式排列（自动换行，不再每项独占一行）
- 澄清：「记录日志类型」tip 明确其控制写入（内存与保存文件），与状态页实时日志的显示筛选互不影响
# v3.0.0_Alpha-11

- 新增：守护通知操作按钮——运行中通知附「关闭」直接停止守护、「结束」终止应用自身（先停各常驻服务抵消 START_STICKY 自动重启，再停 Root AIDL、取消通知，最后干净杀进程，不会"结束变重启"）；未运行常驻通知附「开启」一键启动守护与「结束」按钮（通知按钮属用户交互，豁免 Android 12+ 后台前台服务启动限制）
- 优化：全面精简解释类卡片与功能子注释文案（探测方式/自愈策略/退避上限/执行通道/常驻通知/日志类型/日志位置/统计诊断等 19 处 ×5 语言）——去除冗余修饰与重复解释，保留关键判定规则与操作要点，例如「特权通道用于自愈动作与 ICMP ping 探测（自动=Shizuku→Root→本地逐级回退…）」精简为「仅自愈与 ICMP 探测用特权通道；HTTP/DNS/能力位走应用层无需特权」
# v3.0.0_Alpha-10

- 修复：探测 shell 通道不跟随「执行通道」设置（原 ICMP ping 固定 Shizuku 优先，选"仅系统API"时仍走 Shizuku）——现在 ICMP 探测与自愈动作共用同一「执行通道」设置：自动=Shizuku→Root→本地逐级回退；指定通道不可用时降级应用内本地执行并如实显示（"仅系统API"下 ping 依系统 ping_group_range 放开仍可执行）
- 新增：设置页「执行通道」说明——HTTP 204 / DNS / 系统能力位探测走应用层无需特权，特权通道仅用于自愈动作与 ICMP ping，解答"探测到底用没用 Shizuku/Root"的疑虑
- 新增：状态卡探测结果行（HTTP √204 116ms 等）交互——单行显示不完自动截断省略，点击对应探测项流畅动画展开完整结果（如 ICMP 真实报错多行内容），长按复制该项结果
- 重构：统计页「事件历史」合并为单卡片，并加入与实时日志同款的 5 个操作按钮（复制 / 保存 / 分享 / 管理 / 清空事件）；保存文件名以 guard-events- 前缀与实时日志 guard- 区分；"清空事件"仅清事件保留统计，原"清零"按钮移至统计数字区（全部统计清零）
- 修复：「常驻守护通知」开关开启/关闭效果相同的问题——开关开启时无论守护是否运行都常驻显示通知（守护停止时显示"未运行"，点击进入守护页）；关闭时仅守护运行中显示前台通知（系统前台服务要求）；设置热加载时通知样式即时切换（原改设置后通知不刷新）

# v3.0.0_Alpha-09

- 新增：退避次数限制（熔断器模式，网络调研 AWS Prescriptive Guidance / ByteByteGo 通行做法）——连续自愈失败达到上限（可选 2/3/5/10 次或无限制）后自动停止自愈动作（检测继续），网络恢复在线或手动"立即检测"时自动复位恢复；达到上限时发一次系统通知，避免路由器断电场景下的无限空转轰炸
- 修复：网络自行恢复（如路由器来电）后自愈失败计数不复位，导致退避时间无限叠加、设置次数上限后永远不再自愈（现在在线判定时同步复位熔断计数并记一条恢复日志）
- 新增：自愈策略"自定义档"——自选动作组合（重协商/完整重连/禁用再启用网络/定向重连/WiFi总开关，按由轻到重顺序执行，恢复即停）
- 新增：自愈策略"高成功率档"——动态选取历史累计自愈成功次数最高的单一动作直接执行（按累计成功数排序，成功率作次键，避免小样本 100% 误导）；仅在存在成功统计时出现在下拉菜单，无统计不显示
- 新增：实时日志"正常"筛选，筛选全面升级为多选（正常/异常/自愈可任意组合，"全部"一键全选，全不选显示提示）
- 新增："记录日志类型"设置（取代原"记录成功检测日志"开关）——正常/警告/错误/自愈四类独立开关，默认全部记录，旧版关闭用户自动迁移为"警告+错误+自愈"
- 新增：日志保存位置可配置——默认应用私有 log 目录（免权限），也可唤起系统文件管理器（SAF）自选文件夹并显示位置；已保存日志列表同时管理两个位置的文件并标注来源；SAF 目录失效时自动回退私有目录
- 优化：状态卡"当前通道"行——默认单行截断，点击流畅动画展开逐通道分行显示，长按复制通道内容；探测结果不再重复标注 [Shizuku] 前缀（通道已在状态卡实时展示）
- 新增：自愈执行通道选择未授权通道时自动发起权限申请（选 Shizuku 未授权弹 Shizuku 授权框；选 Root AIDL 未连接拉起 Root 授权）
- 修复：统计页"事件历史"长列表布局异常（文字上方大片留白）——事件列表重构为独立卡片条目，标题行紧凑化

# v3.0.0_Alpha-08

- 修复：ICMP ping 探测恒定失败（exit=2）——根因是 `ping -I wlan0` 走 SO_BINDTODEVICE 套接字选项，该选项需要 CAP_NET_RAW 特权能力（内核 5.6 之前非特权一律被拒），Shizuku 的 shell 用户与应用进程均无此能力；现改为**源 IP 绑定**（`ping -I <WiFi接口IPv4>`，底层普通 bind() 免特权，同样强制探测走 WiFi 防蜂窝回落），源 IP 由 ConnectivityManager 免权限自动获取（同时自适应非 wlan0 命名的设备）
- 修复：ICMP 探测目标 IP 误配（原 204.2.134.20 实为 NTT 网段而非 gstatic）；现改为阿里 DNS/腾讯 DNSPod 公共任播 IP 双目标互备（主 223.5.5.5，备 119.29.29.29），任一无应答自动换备目标重试
- 修复：ICMP 探测失败时只显示 "exit=2" 无法定位；现在显示 ping 的真实报错行，且结果附执行通道（如 [Shizuku]）
- 新增：实时日志全套管理——按级别筛选（全部/仅异常/仅自愈，按级别着色）、一键复制、清空、保存到文件、导出分享（系统分享面板）、已保存日志列表管理（查看/分享/删除/全部清空）
- 新增：记录成功检测日志开关（默认开启）——每轮在线检测都写入实时日志，随时确认守护在正常工作
- 新增：自愈/探测通道全程可见——状态卡显示当前实际使用的通道，自愈动作日志携带通道名，设置页通道项显示可用性（Shizuku ✓/✗），解决"已授权 Shizuku 但不知道是否真的在用"的疑虑
- 修复：状态卡"上次检测 X 秒前"不实时刷新（现每秒滴答更新）

# v3.0.0_Alpha-07

- 新增工具箱「网络守护」：WiFi 断网自动检测与重连，专治"已连接但上不了网"的假连接
- 专业检测引擎（可组合）：HTTP 204 双端点探测（绑定 WiFi 网络防蜂窝回落误判）/ DNS 解析探测（同样绑定 WiFi 网络解析）/ ICMP ping（Shizuku/Root 特权通道，绑定 wlan0 探测固定 IP）/ 系统能力位
- 五级自愈策略（由轻到重，逐级升压探测恢复即停）：重协商 → 断开重连 → 禁用再启用网络 → cmd wifi connect-network 定向重连 → WiFi 总开关
- 自愈执行通道可选：自动（Shizuku→Root AIDL→系统API）/ 仅Shizuku / 仅Root AIDL / 仅系统API（免Root）
- 定时检测：预设 10s~5min 或自定义 5-3600s；网络变化事件即时触发检测
- 前后台常驻：前台服务 + START_STICKY，熄屏后台照常守护
- 防误判三重设计：连续失败防抖阈值、WiFi 链路断开豁免、Captive Portal（认证页）豁免
- 连续自愈失败指数退避（基数可调、倍数封顶），防止路由器断电时空转轰炸
- 完整统计：总检测/断网判定/重连次数/恢复成功率 + 各自愈动作有效率（反推设备根因：重协商有效=链路假死，总开关有效=驱动省电休眠）+ 最近 100 条事件历史
- 自定义间隔对话框、重连成功/失败系统通知、常驻通知开关
- 全部功能设置可配置，覆盖不同设备的各类故障方式；新增 6 语言全部翻译
- 修复：无定位权限时 WiFi 连接状态误判（原用 connectionInfo.ssid 判断连接，Android 9+ 需 ACCESS_FINE_LOCATION 运行时授权，无权限时 SSID 恒为 unknown 导致已连接却被判"未连接"永久跳过检测；现改用 ConnectivityManager 免权限判定，与探测绑网同源）
- 修复：仅打开设置页就会意外启动守护服务（滑块首次组合触发设置写入，热加载意图拉起了未运行的服务）
- 修复：守护运行中但状态卡始终显示"未开始"（检测跳过分支不更新状态与时间；现在跳过时也会如实更新状态）
- 修复：WiFi 假连接回落蜂窝时 DNS 探测走蜂窝成功导致漏检（DNS 解析现已绑定 WiFi 网络）
- 修复：自愈时无定位权限拿不到真实 SSID/networkId（新增特权通道兜底：cmd wifi status / dumpsys wifi）
- 修复：版本号未随发版更新（versionCode 5→6，versionName 对齐 v3.0.0_Alpha-07）

# v3.0.0_Alpha-06

- 支持纯root模式运行（将运行方式全部选择为AidlService，并在全局设置启动AIDL服务）
- 增加多语言支持（ai太好用了你们知道吗）
- 关于页新增一个彩蛋
- 完成一半主页
- wifi运行列表已保存的会有提示以及系统设置保存的密码
- 实验室-获取已保存wifi可查看明文密码（其实是上个版本的）
- 运行历史记录数据格式重构，旧数据会清除
- 运行时显示20次平均速度以及预估剩余时间
- 修复了一个已知BUG
- 新增了亿个未知的BUG
- 还有啥记不清了，自己看吧

# v3.0.0_Alpha-05

- 美化UI
- 完成关于页面
- 密码字典破解增加历史记录功能
- 修复编辑脚本资源后立即查看闪退的问题（好像是重构的时候自己好的？）

# v3.0.0_Alpha-04

- 增加密码本管理、新增脚本类型的字典（非常不完善）
- 修复部分较旧系统版本无法使用
- 新增几个BUG

# v3.0.0_Alpha-03

- 所有功能支持无Shizuku模式（纯系统API）
- 优化运行输出区域布局
- 增加上次尝试成功失败标记
- debug版本增加横幅，并修复无法识别Shizuku的问题

- 以后所有版本使用Github Actions构建，同时增加discussion功能

# v3.0.0_Alpha-02

功能更新：
- 增加异常重试，每次等待一定时间，多次重试无效等待时间翻倍，等待期间不影响其他任务执行（至此运行参数所有内容均生效）
- 实现了一些连接使用系统API连接wifi的方式（虽然有些Bug）
- 重构了部分逻辑
- 增加更新日志

其他：
- 此版本使用Github Actions构建

其他功能正在画饼中……

# v3.0.0_Alpha-01

只能使用Shizuku激活使用，不支持root、不支持无shizuku
主页点左上角菜单打开密码字典破解使用
先在密码字典破解设置里将所有项目改为带shizuku字样的，读取网络日志改为命令行后使用

目前支持：

- 手动输入密码列表运行
- 多任务排队运行、随时新增移除
- 保证ui与服务进度显示统一
- 后台运行（需要调整省电策略为无限制）
- 运行防弹窗密码错误（针对小米部分设备弹窗，运行时循环kill设置）
- 自定义应用主题色、深浅色（应用设置页修改）

不支持/未完成：

- root模式/普通模式
- 修改运行参数
- 除密码字典破解、实验室、设置以外的其他页面
- 密码本管理