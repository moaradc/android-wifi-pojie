# v3.0.0_Alpha-07

- 新增工具箱「网络守护」：WiFi 断网自动检测与重连，专治"已连接但上不了网"的假连接
- 专业检测引擎（可组合）：HTTP 204 双端点探测（绑定 WiFi 网络防蜂窝回落误判）/ DNS 解析探测（同样绑定 WiFi 网络解析）/ ICMP ping（Shizuku/Root 特权通道，绑定 wlan0 探测固定 IP）/ 系统能力位
- 五级自愈策略（由轻到重，逐级升压探测恢复即停）：重协商 → 断开重连 → 禁用再启用网络 → cmd wifi connect-network 定向重连 → WiFi 总开关
- 自愈执行通道可选：自动（Shizuku→Root AIDL→系统API）/ 仅Shizuku / 仅Root AIDL / 仅系统API（免Root）
- 定时检测：预设 10s~5min 或自定义 5-3600s；网络变化事件即时触发检测
- 前后台常驻：前台服务 + START_STICKY，熄屏后台照常守护
- 防误判三重设计：连续失败防抖阈值、WiFi 链路断开豁免、Captive Portal（认证页）豁免
- 连续自愈失败指数退避（基数可调、倍数封顶），防止路由器断电时空转轰炸
- 完整统计：总检测/断网判定/重连次数/恢复成功率 + 各自愈动作有效率（反推设备根因：重协商有效=链路假死，总开关有效=驱动省电休眠）+ 最近 100 条事件历史
- 自定义间隔对话框、重连成功/失败系统通知、常驻通知开关
- 全部功能设置可配置，覆盖不同设备的各类故障方式；新增 6 语言全部翻译
- 修复：无定位权限时 WiFi 连接状态误判（原用 connectionInfo.ssid 判断连接，Android 9+ 需 ACCESS_FINE_LOCATION 运行时授权，无权限时 SSID 恒为 unknown 导致已连接却被判"未连接"永久跳过检测；现改用 ConnectivityManager 免权限判定，与探测绑网同源）
- 修复：仅打开设置页就会意外启动守护服务（滑块首次组合触发设置写入，热加载意图拉起了未运行的服务）
- 修复：守护运行中但状态卡始终显示"未开始"（检测跳过分支不更新状态与时间；现在跳过时也会如实更新状态）
- 修复：WiFi 假连接回落蜂窝时 DNS 探测走蜂窝成功导致漏检（DNS 解析现已绑定 WiFi 网络）
- 修复：自愈时无定位权限拿不到真实 SSID/networkId（新增特权通道兜底：cmd wifi status / dumpsys wifi）
- 修复：版本号未随发版更新（versionCode 5→6，versionName 对齐 v3.0.0_Alpha-07）

# v3.0.0_Alpha-06

- 支持纯root模式运行（将运行方式全部选择为AidlService，并在全局设置启动AIDL服务）
- 增加多语言支持（ai太好用了你们知道吗）
- 关于页新增一个彩蛋
- 完成一半主页
- wifi运行列表已保存的会有提示以及系统设置保存的密码
- 实验室-获取已保存wifi可查看明文密码（其实是上个版本的）
- 运行历史记录数据格式重构，旧数据会清除
- 运行时显示20次平均速度以及预估剩余时间
- 修复了一个已知BUG
- 新增了亿个未知的BUG
- 还有啥记不清了，自己看吧

# v3.0.0_Alpha-05

- 美化UI
- 完成关于页面
- 密码字典破解增加历史记录功能
- 修复编辑脚本资源后立即查看闪退的问题（好像是重构的时候自己好的？）

# v3.0.0_Alpha-04

- 增加密码本管理、新增脚本类型的字典（非常不完善）
- 修复部分较旧系统版本无法使用
- 新增几个BUG

# v3.0.0_Alpha-03

- 所有功能支持无Shizuku模式（纯系统API）
- 优化运行输出区域布局
- 增加上次尝试成功失败标记
- debug版本增加横幅，并修复无法识别Shizuku的问题

- 以后所有版本使用Github Actions构建，同时增加discussion功能

# v3.0.0_Alpha-02

功能更新：
- 增加异常重试，每次等待一定时间，多次重试无效等待时间翻倍，等待期间不影响其他任务执行（至此运行参数所有内容均生效）
- 实现了一些连接使用系统API连接wifi的方式（虽然有些Bug）
- 重构了部分逻辑
- 增加更新日志

其他：
- 此版本使用Github Actions构建

其他功能正在画饼中……

# v3.0.0_Alpha-01

只能使用Shizuku激活使用，不支持root、不支持无shizuku
主页点左上角菜单打开密码字典破解使用
先在密码字典破解设置里将所有项目改为带shizuku字样的，读取网络日志改为命令行后使用

目前支持：

- 手动输入密码列表运行
- 多任务排队运行、随时新增移除
- 保证ui与服务进度显示统一
- 后台运行（需要调整省电策略为无限制）
- 运行防弹窗密码错误（针对小米部分设备弹窗，运行时循环kill设置）
- 自定义应用主题色、深浅色（应用设置页修改）

不支持/未完成：

- root模式/普通模式
- 修改运行参数
- 除密码字典破解、实验室、设置以外的其他页面
- 密码本管理